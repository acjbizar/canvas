{
    "lang": "{{ artwork.lang }}",
    "dir": "{{ artwork.dir }}",
    "name": "{{ artwork.title }}",
    "icons": [{
        "src": "{{ artwork.url }}apple-touch-icon-precomposed.png"
    }],
    "scope": "/",
    "id": "{{ artwork.url }}",
    "theme_color": "{{ artwork.themeColor }}"
}